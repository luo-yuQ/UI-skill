interface:
  display_name: "Game UI Asset Analyzer"
  short_description: "Route UI nodes and analyze reusable visual assets"
  default_prompt: "Use $game-ui-asset-analyzer to route this UI node, then analyze reusable visual assets when the deterministic action selects semantic decomposition."
