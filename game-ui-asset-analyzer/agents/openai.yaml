interface:
  display_name: "Game UI Asset Analyzer"
  short_description: "Route, structurally split, and analyze game UI nodes"
  default_prompt: "Use $game-ui-asset-analyzer to route this UI node and apply the selected frozen structural-split or semantic-decomposition contract."
