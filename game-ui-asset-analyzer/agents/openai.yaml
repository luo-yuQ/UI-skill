interface:
  display_name: "Game UI Asset Analyzer"
  short_description: "Route, split, expand, and analyze game UI nodes"
  default_prompt: "Use $game-ui-asset-analyzer to route this UI node and apply the selected frozen structural-split, instance-expansion, or semantic-decomposition contract."
