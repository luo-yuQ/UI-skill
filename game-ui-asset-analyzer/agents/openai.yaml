interface:
  display_name: "Game UI Asset Analyzer"
  short_description: "Decompose reusable UI assets and map their bboxes"
  default_prompt: "Use $game-ui-asset-analyzer to decompose this game UI into reusable visual asset candidates and map their bboxes to source pixels."
