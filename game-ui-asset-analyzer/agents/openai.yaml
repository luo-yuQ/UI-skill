interface:
  display_name: "Game UI Asset Analyzer"
  short_description: "Map analyzed UI bboxes to source-image pixels"
  default_prompt: "Use $game-ui-asset-analyzer to prepare a deterministic analysis image, identify asset candidates, and map their bboxes back to the source image."
